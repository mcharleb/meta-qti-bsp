#@TYPE: Machine
#@NAME: MDM9640 Perf
#@DESCRIPTION: Machine configuration for QTI MDM9640 for perf.

BASEMACHINE = "mdm9640"

include ${BASEMACHINE}.conf

MACHINEOVERRIDES = "${BASEMACHINE}:${MACHINE}"
